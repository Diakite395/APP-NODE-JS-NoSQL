const liste1 = [1, 2, 3];
/**
 * La methode concat() ne modifie pas liste mais renvoie une liste avec la nouvelle valeur ajouter
 * La methode push() modifie la liste et retourne la valeur ajouter a la liste
 */
const liste2 = liste1.concat(5);

console.log('Liste1:', liste1);
console.log('Liste2:', liste2);
